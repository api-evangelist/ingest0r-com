generated: '2026-09-19'
method: searched
source: https://api.ingest0r.com/changelog.json
raw_file: ingest0r-com-changelog.json
description: >-
  Structured capture of the provider's machine-readable changelog (JSON, linked from the sitemap, llms.txt and
  the agent card's x-links). Six dated semantic-version entries from the initial public release to the current
  0.4.2; each entry is a list of tagged change lines (ADD / FIX / HARDEN / PRICE / NOTE). Captured in full because
  the whole history is six entries; the live document is the source of truth.
docs: https://api.ingest0r.com/changelog.json
versioning:
  scheme: semantic schema version, additive-only within /v1
  header: X-Schema-Version
  current: 0.4.2
  policy: 'Additive-only within /v1: existing fields never change type or meaning and are never removed; new fields may appear. Breaking changes would ship as /v2 with /v1 kept alive.'
  detail: lifecycle/ingest0r-com-lifecycle.yml
entries:
- version: 0.4.2
  date: '2026-08-19'
  latest: true
  breaking: []
  additions:
  - Free tier — the first 25 paid-route calls per client per day return real data with no payment; X-Free-Tier-Remaining / X-Free-Tier headers report standing; prices, 402 body and payment terms unchanged
  notes:
  - The free tier never applies to the directory-probe PINs or route-template URLs, which continue to return 402 so listing verification is unaffected
- version: 0.4.1
  date: '2026-08-05'
  breaking: []
  additions:
  - MCP payment interop with the agents-SDK x402 convention — paid tools return the 402 under _meta["x402/error"], accept payment via _meta["x402/payment"], surface receipts as _meta["x402/payment-response"], carry agents-x402/priceUSD tool _meta
  fixes:
  - 402 accepts.extra and discovery-extension strings made pure ASCII (em dashes had broken btoa()-based x402 clients)
  hardening:
  - Payment-header guards (HEAD never settles, size cap, replay pre-check) extended to the x402 v2 PAYMENT-SIGNATURE header as well as legacy X-PAYMENT
- version: 0.4.0
  date: '2026-07-17'
  breaking: []
  price_changes:
  - /v1/search is now FREE (was $0.01); PIN routes remain pay-per-call
  hardening:
  - HEAD requests never settle a payment; root cache private; q-value-aware content negotiation; POST / answers JSON-RPC instead of 405
- version: 0.3.0
  date: '2026-07-12'
  breaking: []
  additions:
  - Per-response provenance — as_of, source, cite
  - Free live example PINs (golden PINs) on paid routes
  - /pricing, /changelog.json, /llms-full.txt, HTML landing
  - X-Schema-Version header; HEAD support; ETag caching on discovery surfaces
  fixes:
  - /v1/search short-query error body (was invalid_pin, now query_too_short)
- version: 0.2.0
  date: '2026-07-05'
  breaking: []
  additions:
  - llms.txt, agents.txt, sitemap, robots, security.txt, favicon, /.well-known/{agent.json,mcp,mcp.json}
  - Curated /openapi.json with x-payment-info
- version: 0.1.0
  date: '2026-06-13'
  breaking: []
  additions:
  - 'Initial public release: /v1/{search,parcel,dossier,comps} behind x402; /v1/sample free; A2A card; x402 resource list'
